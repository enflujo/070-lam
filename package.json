{
  "name": "enflujo-lam",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "aplicaciones/*"
  ],
  "scripts": {
    "dev": "yarn workspace www dev",
    "build": "yarn workspace www build",
    "preview": "yarn workspace www preview",
    "lint": "yarn workspace www lint",
    "lint:fix": "yarn workspace www lint:fix",
    "datos": "yarn workspace extractor dev"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
